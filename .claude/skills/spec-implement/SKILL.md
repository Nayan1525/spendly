---
name: spec-implement
description: Drive a feature through spec → test → implement. Use when the user asks to build/implement a new feature end-to-end and wants it delivered with tests and a scope check, not just an ad-hoc edit.
---

# Spec → Test → Implement
1. Write a spec markdown for the feature
2. Write/adjust tests first
3. Implement across files
4. Run the test pipeline and report pass/fail counts
Never edit unrelated tests. Confirm scope before large changes.
